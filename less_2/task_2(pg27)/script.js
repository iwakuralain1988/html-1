function linearEquationsSolver(a, b) {
    let result = -b / a;
    return result
}

linearEquationsSolver(7, 8)

function quadEquationsSolver(a, b, c) {
    d = Math.pow(2) - 4 * 1 * 1
    let result1 = -1 + Math.sqrt(parseInt(d)) / 2
    let result2 = -1 + Math.sqrt(parseInt(d)) / 2
    return result1;
}